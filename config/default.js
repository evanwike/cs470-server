// Replace <values> with your Azure SQL Server login credentials
const config = {
  "db": {
    "user": "<username>",
    "password": "<password>",
    "server": "<server>",
    "database": "<database>"
  }
}

module.exports = config;
